const primary = "#540e4fff"
const primaryDark = "#3b0638ff"
const clearColor = "white"
const secondary = "#9b5de5"

export const theme = {
    primary: primary,
    primaryDark: primaryDark,
    secondary: secondary,
    textColor: {
        light: clearColor,
        dark: primaryDark
    }
}